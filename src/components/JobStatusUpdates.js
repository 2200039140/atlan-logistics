import React from 'react';

const JobStatusUpdates = () => {
  return (
    <div>
      <h2>Job Status Updates</h2>
      <p>Drivers can update their job status here (e.g., En Route, Goods Collected, Delivered).</p>
      {/* Logic for drivers to update status */}
    </div>
  );
};

export default JobStatusUpdates;
